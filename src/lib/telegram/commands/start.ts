import { BotContext } from '../bot';

export async function handleStartCommand(ctx: BotContext) {
  const name = ctx.from?.first_name || 'пользователь';

  if (!ctx.user) {
    await ctx.reply(
      `Привет, ${name}! 👋\n\n` +
        `Добро пожаловать в GoalFlow - систему управления целями.\n\n` +
        `Для начала работы необходимо связать ваш Telegram аккаунт с аккаунтом GoalFlow.\n\n` +
        `Используйте команду /link для получения инструкций по привязке аккаунта.`,
    );
    return;
  }

  await ctx.reply(
    `Привет, ${ctx.user.name || name}! 👋\n\n` +
      `Вы успешно подключены к GoalFlow.\n\n` +
      `Доступные команды:\n` +
      `/goals - список ваших целей\n` +
      `/report - отчет по целям\n` +
      `/help - помощь\n\n` +
      `Используйте /help для подробной информации.`,
  );
}

