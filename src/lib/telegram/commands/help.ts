import { BotContext } from '../bot';

export async function handleHelpCommand(ctx: BotContext) {
  const helpText =
    `📚 *Справка по командам GoalFlow*\n\n` +
    `*Основные команды:*\n` +
    `/start - Начать работу с ботом\n` +
    `/help - Показать эту справку\n` +
    `/goals - Показать список ваших целей\n` +
    `/report - Получить отчет по целям\n` +
    `/link - Инструкции по привязке аккаунта\n\n` +
    `*Как использовать:*\n` +
    `1. Привяжите ваш Telegram аккаунт через команду /link\n` +
    `2. Используйте команды для просмотра целей и отчетов\n` +
    `3. Уведомления о дедлайнах и изменениях будут приходить автоматически\n\n` +
    `*Поддержка:*\n` +
    `Если у вас возникли вопросы, обратитесь к администратору системы.`;

  await ctx.reply(helpText, { parse_mode: 'Markdown' });
}

